# MindCanvas v5.6.0 — Wiki tính năng & hướng dẫn lần đầu

## Mục tiêu

v5.6.0 thêm một Wiki trong ứng dụng để người dùng khám phá các tính năng bằng thẻ hướng dẫn, GIF minh họa và overlay tương tác. Overlay làm tối giao diện, khoanh sáng đúng điều khiển cần bấm và hiển thị con trỏ chuyển động hướng vào điều khiển đó.

## Wiki tính năng

- Mở `Hướng dẫn tính năng` trên Sidebar.
- Tìm kiếm theo từ khóa và lọc theo Workspace, Canvas, Học tập, Tài liệu hoặc AI.
- Mỗi thẻ có GIF minh họa, mô tả ngắn, trạng thái và nút `Chạy hướng dẫn`.
- GIF được lưu tại `apps/web/public/guides/` để chạy offline cùng bản web.

## Hướng dẫn lần đầu

Khi người dùng lần đầu mở Canvas, Trung tâm học tập, Lab hoặc Quản lý thư mục, guide tương ứng sẽ xuất hiện sau một khoảng chờ ngắn để giao diện ổn định. Người dùng bấm trực tiếp vào vùng đang được khoanh sáng để chuyển bước; nếu điều khiển không còn trong DOM thì nút `Tiếp tục tại đây` sẽ được hiển thị.

Trạng thái được lưu theo khóa localStorage `mindcanvas:feature-guides:v1:<owner-or-guest>`. Các trạng thái gồm `unseen`, `active`, `completed` và `skipped`, vì vậy việc reset của admin không làm ảnh hưởng project, Lab, tài liệu hay canvas.

## Admin

Phần `Trạng thái hướng dẫn tính năng` nằm cuối trang Admin. Mỗi dòng hiển thị:

- ô tích đã/chưa kích hoạt;
- thời điểm bắt đầu;
- schema hiện tại (`v1`);
- nút thử guide và nút reset riêng.

`Đặt lại tất cả` đưa toàn bộ guide của tài khoản admin hiện tại về `Chưa kích hoạt`, thuận tiện cho QA và kiểm thử onboarding.

## Kiểm thử và accessibility

- Overlay dùng `role="dialog"`, nút bỏ qua và nút đóng.
- `prefers-reduced-motion` tắt các animation chuyển động.
- Selector guide dùng class semantic ổn định thay vì cấu trúc React tự sinh.
- GIF có fallback minh họa CSS nếu trình duyệt không tải được ảnh.

