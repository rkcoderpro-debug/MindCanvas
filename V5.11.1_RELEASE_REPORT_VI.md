# MindCanvas v5.11.1 — Báo cáo sửa lỗi lưu/tải Cloud

## Nguyên nhân

Ảnh đính kèm ghi lỗi `localStorage.setItem` hết quota ở cache danh sách flashcard. Các yêu cầu Network đang thấy trả `200`; do đó dấu hiệu cụ thể không chỉ ra lỗi RLS hay Supabase. Trước bản vá, lỗi ghi cache nằm trong cùng nhánh `try/catch` với thao tác cloud, làm dữ liệu cloud mới bị thay bằng cache cục bộ hoặc khiến thao tác lưu đã thành công bị báo lỗi.

## Thay đổi

- Cloud là kết quả chính cho thao tác đọc/ghi đã được Supabase xác nhận; ghi cache cục bộ trở thành best-effort.
- Khi cache không ghi được, bản cache mới được giữ trong bộ nhớ của phiên hiện tại và phân tách theo khóa tài khoản.
- Lưu cục bộ khi không có cloud sẽ báo hết dung lượng nếu không thể ghi bền vững.
- Tăng phiên bản gói từ 5.11.0 lên 5.11.1 và đổi khóa cache Service Worker.
- Không đổi schema hoặc chạy migration.

## Xác minh

- Đã thêm 5 kiểm thử hồi quy. Chúng xác minh việc tải bộ/thẻ, giữ cache mới trong phiên, lưu bộ/lô thẻ cloud khi `setItem` ném `QuotaExceededError`, và chặn thông báo lưu cục bộ giả thành công.
- Web: 51 tệp kiểm thử, 336 kiểm thử đạt. Server: 42 kiểm thử đạt.
- Typecheck web/server đạt. Build production web/server đạt. `git diff --check` đạt.
- Build vẫn hiển thị cảnh báo chunk JavaScript lớn hơn 500 kB; build hoàn tất thành công. Đây là cảnh báo đã có trong dự án, không liên quan đến bản vá cache.
- Chưa kiểm tra Supabase thật, staging, trình duyệt đã đăng nhập hoặc triển khai production. Site trong ảnh đang hiện v5.10.1; cần xác nhận phiên bản sau khi cập nhật ứng dụng.

## Gói bàn giao

`MindCanvas-v5.11.1-cloud-storage-fix-changed-files.zip` chứa các file thay đổi với tiền tố `mindcanvas-v1/`. Đây là gói chồng lên mã nguồn MindCanvas v5.11.0, không phải toàn bộ repository.
