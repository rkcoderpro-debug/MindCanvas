# MindCanvas V5.12.0 — lưu học liệu được chia sẻ vào thư viện cá nhân

## Nội dung

- Thêm thao tác **Lưu vào của tôi** cho Quiz, Flashcard, Lab và Tài liệu trong mục **Được chia sẻ với tôi**.
- Mỗi thao tác tạo bản sao độc lập thuộc tài khoản người nhận. Bản sao vẫn dùng được khi chủ sở hữu chỉnh sửa, thu hồi chia sẻ hoặc xóa học liệu gốc.
- Quiz, Flashcard và Lab xuất hiện trong tab riêng của người nhận; Tài liệu xuất hiện trong thư viện Tài liệu và được tải về thiết bị khi mở.
- Thêm dấu “Đã lưu từ…” và bộ lọc bản đã lưu trong Quiz/Flashcard. Tiến độ Flashcard của bản sao được khởi tạo riêng.
- Tài liệu được sao chép trong Supabase Storage, rồi đăng ký thành tài liệu của người nhận. Dung lượng sẽ tính vào quota Storage của tài khoản nhận.
- Nếu phản hồi đăng ký bị rớt, thao tác được kiểm tra và thử lại theo khóa idempotent. Khi trạng thái vẫn không xác định do mất mạng, object được giữ lại để tránh bản ghi đã lưu trỏ đến tệp bị xóa; tải lại danh sách trước khi bấm lưu lại.
- Quyền chia sẻ: Quiz/Flashcard yêu cầu Plus trở lên; Lab/Tài liệu yêu cầu Pro trở lên. Người nhận không cần nâng gói để lưu bản sao.

## Triển khai

1. Sao lưu database theo quy trình hiện tại.
2. Trong Supabase SQL Editor, chạy toàn bộ `supabase/migrations/0023_v5_12_0_save_shared_learning.sql` sau migration 0022. Không chạy lại các migration đã áp dụng.
3. Triển khai các tệp trong `CHANGED_FILES_V5.12.0.txt`, build và phát hành web.
4. Mở lại web/PWA và xác nhận badge hiển thị **V5.12.0**. Service worker dùng cache key mới để tải bundle phát hành.

## Kiểm tra sau phát hành

1. Tài khoản Plus chia sẻ một Quiz và một bộ Flashcard; tài khoản Pro chia sẻ một Lab và một Tài liệu.
2. Người nhận mở **Được chia sẻ với tôi**, lưu từng loại và xác nhận trạng thái **Đã lưu vào của tôi**.
3. Mở từng bản từ tab Quiz, Flashcard, Lab và thư viện Tài liệu; xác nhận bản sao có nhãn nguồn và hoạt động như học liệu cá nhân.
4. Sửa hoặc thu hồi nguồn từ tài khoản gửi, tải lại tài khoản nhận; xác nhận bản sao vẫn mở và chỉnh sửa độc lập.
5. Với Tài liệu, xác nhận file mở được và dung lượng Storage của người nhận tăng đúng kích thước file. Khi quota đầy, thao tác phải báo lỗi và không để lại bản ghi hay object thừa.
6. Thử tài khoản Free làm người nhận: có thể lưu bản sao; quyền chia sẻ vẫn được quyết định theo gói của người gửi.

## Xác minh trong môi trường phát triển

- `npm test`
- `npm run typecheck`
- `npm run build`

Migration chưa được áp dụng lên Supabase thật trong quá trình tạo gói này; cần chạy 0023 tại môi trường mục tiêu trước khi tính năng hoạt động trên web đã triển khai.
