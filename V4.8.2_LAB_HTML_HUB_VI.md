# MindCanvas v4.8.2 — Lab HTML và Trung tâm học tập

## Phạm vi

V4.8.2 điều chỉnh luồng Lab theo đúng cách sử dụng Manual:

1. Người dùng nhập tài liệu nguồn, trình độ và yêu cầu mô phỏng.
2. MindCanvas tạo một prompt duy nhất để người dùng sao chép sang AI. Prompt yêu cầu AI đọc tài liệu và trả về một file HTML mô phỏng tự chứa, có mô hình khoa học, tương tác và bộ kiểm thử xác định.
3. Người dùng tải file HTML do AI trả về hoặc dán nội dung vào Lab, sau đó kiểm tra và chạy trong iframe sandbox.

Bước dán prompt kế hoạch trung gian đã được bỏ khỏi giao diện. Các trường `planPrompt`, `programPrompt` và dữ liệu JSON cũ vẫn được đọc để không làm mất Lab đã lưu từ bản trước.

## Sửa lỗi HTML hợp lệ

Bộ kiểm tra trước đây nhận nhầm namespace SVG `http://www.w3.org/2000/svg` là URL tài nguyên ngoài. V4.8.2 cho phép các namespace W3C chuẩn dùng cho SVG/XLink/XHTML nhưng vẫn chặn:

- script, stylesheet, iframe, object, embed và form tải hoặc điều hướng ngoài;
- URL ngoài và API mạng;
- truy cập `top`, `parent` hoặc `opener`.

File `mindcanvas_shm_signs.html` có namespace SVG này và đã được kiểm tra không còn bị báo lỗi `unsafeHtml`.

## Điều hướng

Lab đã trở thành tab nổi bật trong Trung tâm học tập, có nhãn MỚI và nút truy cập nhanh từ phần Tổng quan. Nút Lab riêng trên sidebar đã được bỏ. Đường dẫn `__lab` cũ vẫn mở Trung tâm học tập tại tab Lab.

## Kiểm thử

- Web tests: kiểm tra prompt HTML trực tiếp, namespace SVG, chặn tài nguyên/API ngoài, lưu Lab tương thích và chạy iframe sandbox.
- App tests: mở Lab qua Trung tâm học tập, tạo prompt HTML, dán HTML và chạy preview trong iframe `sandbox="allow-scripts"`.
- File mẫu: HTML tự chứa kích thước khoảng 51 KB được bộ lọc chấp nhận.

## Giới hạn

Lab vẫn lưu cục bộ theo tài khoản/trình duyệt ở bản này; chưa thêm đồng bộ Lab lên Supabase. Việc kiểm tra hiện xác nhận cấu trúc và quy tắc chạy an toàn của HTML, chưa chứng minh tính đúng đắn khoa học của mọi mô phỏng.
