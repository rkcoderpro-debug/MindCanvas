import { useState } from "react";
import { Clipboard, Copy, FileText, Folder, FolderPlus, MoreHorizontal, Move } from "lucide-react";
import type { Project, ProjectFolder, ProjectPatch } from "../lib/projectStore";
import { useLanguage } from "../lib/i18n";
import Dialog from "./Dialog";

export default function FolderManager({ projects, folders, onOpen, onManage, onDuplicate, onRenameFolder, onDeleteFolder, onCreateFolder }: { projects: Project[]; folders: ProjectFolder[]; onOpen:(p:Project)=>void; onManage:(p:Project,patch:ProjectPatch)=>Promise<void>; onDuplicate:(p:Project,title:string,target?:string|null)=>Promise<void>; onRenameFolder:(f:ProjectFolder,name:string)=>Promise<void>; onDeleteFolder:(f:ProjectFolder)=>Promise<void>; onCreateFolder:()=>void }) {
  const { t } = useLanguage(); const [active,setActive]=useState<string|null>(null), [selected,setSelected]=useState<Project|null>(null), [clipboard,setClipboard]=useState<Project|null>(null), [dialog,setDialog]=useState<"rename"|"move"|"delete"|null>(null), [value,setValue]=useState(""), [busy,setBusy]=useState(false);
  const list=projects.filter(p=>!p.deletedAt && (active===null || p.folderId===active)); const folderName=active?folders.find(f=>f.id===active)?.name:t("workspace");
  const run=async(fn:()=>Promise<void>)=>{setBusy(true);try{await fn();setDialog(null);setSelected(null);}finally{setBusy(false);}};
  return <section className="folder-manager"><div className="folder-manager-side"><div className="manager-title"><strong>{t("folders")}</strong><button className="icon-button" aria-label={t("newFolder")} onClick={onCreateFolder}><FolderPlus size={17}/></button></div><button className={active===null?"manager-folder active":"manager-folder"} onClick={()=>setActive(null)}><Folder size={17}/>{t("workspace")}</button>{folders.map(f=><button key={f.id} className={active===f.id?"manager-folder active":"manager-folder"} onClick={()=>setActive(f.id)}><Folder size={17}/><span>{f.name}</span><small>{projects.filter(p=>p.folderId===f.id&&!p.deletedAt).length}</small></button>)}</div><div className="folder-manager-main"><div className="manager-toolbar"><div><span className="eyebrow">{t("folders")}</span><h1>{folderName}</h1></div><div className="actions"><button className="secondary-button" disabled={!selected} onClick={()=>selected&&setClipboard(selected)}><Copy size={16}/>{t("copy")}</button><button className="secondary-button" disabled={!clipboard} onClick={()=>clipboard&&void run(()=>onDuplicate(clipboard,`${clipboard.title} — ${t("copySuffix")}`,active))}><Clipboard size={16}/>{t("paste")}</button><button className="secondary-button" disabled={!selected} onClick={()=>{if(selected){setValue(selected.folderId??"");setDialog("move")}}}><Move size={16}/>{t("move")}</button><button className="primary-button" disabled={!selected} onClick={()=>selected&&void run(()=>onDuplicate(selected,`${selected.title} — ${t("copySuffix")}`,active))}>{t("duplicate")}</button></div></div>{!list.length?<div className="empty-state"><Folder size={42}/><h2>{t("emptyFolder")}</h2></div>:<div className="file-table">{list.map(p=><div className={selected?.id===p.id?"file-row selected":"file-row"} key={p.id} onClick={()=>setSelected(p)} onDoubleClick={()=>onOpen(p)}><FileText size={19}/><span>{p.title}</span><small>{new Date(p.updatedAt).toLocaleDateString()}</small><button className="icon-button" aria-label={`${t("fileActions")}: ${p.title}`} onClick={e=>{e.stopPropagation();setSelected(p);setValue(p.title);setDialog("rename")}}><MoreHorizontal size={18}/></button></div>)}</div>}</div>{dialog&&selected&&<Dialog title={t(dialog)} onClose={()=>setDialog(null)}>{dialog==="rename"&&<form onSubmit={e=>{e.preventDefault();void run(()=>onManage(selected,{title:value.trim()}))}}><input autoFocus required value={value} onChange={e=>setValue(e.target.value)}/><footer className="actions"><button type="button" onClick={()=>setDialog(null)}>{t("cancel")}</button><button className="primary-button" disabled={busy}>{t("save")}</button></footer></form>}{dialog==="move"&&<form onSubmit={e=>{e.preventDefault();void run(()=>onManage(selected,{folderId:value||null}))}}><select value={value} onChange={e=>setValue(e.target.value)}><option value="">{t("noFolder")}</option>{folders.map(f=><option key={f.id} value={f.id}>{f.name}</option>)}</select><footer className="actions"><button type="button" onClick={()=>setDialog(null)}>{t("cancel")}</button><button className="primary-button">{t("save")}</button></footer></form>}{dialog==="delete"&&<><p>{t("deleteFileHint")}</p><button className="danger-button" onClick={()=>void run(()=>onManage(selected,{deletedAt:new Date().toISOString()}))}>{t("delete")}</button></>}</Dialog>}</section>;
}
