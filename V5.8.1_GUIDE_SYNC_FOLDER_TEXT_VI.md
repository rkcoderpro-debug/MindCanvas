# MindCanvas v5.8.1 — đồng bộ guide, quản lý thư mục và sửa text

## Đã vá

- Khi người dùng bấm Tạo trực tiếp trong hộp nhập, guide nhận action tạo thành công và tự bỏ qua bước nhập cũ, kể cả khi dialog hoặc route thay đổi. Chỉ action sau khi tạo thật mới được ghi nhận.
- Guide không còn tự gọi hoàn tất ở bước action cuối; người dùng vẫn nhìn thấy trạng thái đã xong và bấm Tiếp tục rõ ràng.
- Popover guide được giới hạn theo viewport, có cuộn nội bộ và không bị tràn/cắt chữ ở màn hình hẹp.
- Quản lý thư mục có nút thao tác cho từng thư mục: đổi tên và Xóa thư mục. Khi xóa, project trong thư mục được đưa về Workspace theo hành vi hiện có.
- Đổi tên project/thư mục phát action sau khi lưu thành công để các guide đang mở không giữ target cũ.
- Double-click chữ dùng công cụ chọn hiện tại (kể cả đang giữ V tạm thời) để mở trình sửa inline; Hand/Space và các công cụ vẽ không bị cướp thao tác.

## Kiểm tra

- TypeScript `tsc --noEmit`.
- Unit tests cho guide, canvas interaction và double-click text.
- Production build Vite.

