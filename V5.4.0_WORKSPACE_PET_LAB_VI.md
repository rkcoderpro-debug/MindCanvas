# MindCanvas v5.4.0

Bản vá dành cho mã nguồn v5.3.0 được cung cấp trong mindcanvas-v1 (2).zip.

## Cài đặt

1. Sao lưu mã nguồn hiện tại.
2. Giải nén và chép đè thư mục `mindcanvas-v1` vào đúng thư mục cùng tên. Nếu repository của bạn có tên khác, chép nội dung bên trong `mindcanvas-v1` vào thư mục gốc repository.
3. Chạy `npm ci`, sau đó `npm run build` và triển khai theo quy trình hiện tại.
4. Không cần chạy SQL mới. Không xóa dữ liệu trình duyệt khi cập nhật.

ZIP chỉ chứa file nguồn mới/thay đổi, giữ nguyên đường dẫn. Không có node_modules, dist hoặc dữ liệu người dùng.

## Thay đổi

- Workspace có dropdown File gần đây / Yêu thích / Thùng rác, mặc định đóng. Thư mục chiếm phần chiều cao còn lại.
- Sửa người dùng mới bị ép về 154px; mặc định đúng 280px. Dải kéo thanh bên luôn hiện khi mở trên desktop.
- Vẽ lại mặt và tai chó, chỉnh màu pet và chuyển động tai/đầu/mắt. Phạm vi nhìn bao phủ cửa sổ.
- Kéo pet bằng chuột/cảm ứng, lưu vị trí, giới hạn trong màn hình. Cài đặt có bật/tắt pet và đặt lại vị trí. Xoa đầu khi rê chuột vẫn hoạt động.
- Touchpad giữ nguyên hai trục chuẩn, không thay giá trị 0 hợp lệ bằng wheel delta cũ.
- Prompt Lab yêu cầu file HTML tải xuống thật; có phương án trả mã khi AI không hỗ trợ file, cấm bịa liên kết.
- Lưu Lab bằng IndexedDB với transaction. Chỉ báo thành công khi ghi hoàn tất. Dữ liệu localStorage cũ được chuyển một lần và giữ nguyên để phục hồi.
- Không tự cắt danh sách khi vượt 40 Lab; không tự cắt HTML khi đọc dữ liệu cũ.
- Có bản nháp theo tài khoản, cảnh báo thay thế nội dung chưa lưu, ngăn bấm lưu lặp. Lưu HTML trực tiếp không cần bước prompt.
- Nút Lưu bám vùng nhìn và có trong thanh toàn màn hình. Lỗi/thông báo lưu có trong toàn màn hình.
- Lưu cloud/chia sẻ yêu cầu lưu thay đổi trên thiết bị trước để tránh đưa bản cũ lên cloud.

## Kiểm tra

- TypeScript và production build web đạt.
- 93 kiểm thử liên quan đạt: App, CanvasBoard, labStorage, lab, canvasViewport, sidebarLayout, pet.
- Trình duyệt Chromium: dropdown; chọn chó; kéo pet; lưu Lab chỉ có HTML; tải lại trang vẫn giữ nội dung; xem bố cục 1366x900 và 390x844. Không có lỗi JavaScript trong lượt này.
- Kiểm thử IndexedDB dùng fake-indexeddb (dev dependency mới): cập nhật cùng ID, tách tài khoản, chuyển dữ liệu 45 Lab, không phục hồi lại Lab đã xóa, báo lỗi khi storage thất bại, bản nháp và dữ liệu cũ hỏng.

## Giới hạn cần biết

- Chưa kiểm tra touchpad vật lý trên máy người dùng hoặc Safari iOS thực tế; các sự kiện pan đã được kiểm tra bằng test.
- Lưu trên thiết bị vẫn phụ thuộc dữ liệu trình duyệt. Xóa dữ liệu website có thể xóa Lab; nút tải HTML và luồng lưu cloud hiện có vẫn được giữ.
- Chưa kiểm thử với tài khoản cloud thật; không thay đổi schema hoặc quyền gói.
- Một số AI không hỗ trợ tạo file; prompt không thể tự thêm khả năng đó.
