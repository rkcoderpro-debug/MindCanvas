# MindCanvas V5.11.6 — bảo vệ canvas khi bộ nhớ trình duyệt chạm quota

## Kết luận từ ảnh Chrome

Ảnh Storage cho thấy khoảng 2,0 MB đang dùng trên hạn mức 322.252 MB, trong đó Cache Storage khoảng 2,0 MB và IndexedDB khoảng 64,5 kB. Đây **không phải** bằng chứng quota tổng hoặc cloud đã đầy. `localStorage` có giới hạn riêng (thường khoảng 5 MiB mỗi origin) và không thể nâng bằng cấu hình website. Ảnh này cũng không đo quota của Supabase.

Không xóa dữ liệu website, IndexedDB hay localStorage khi còn canvas/flashcard chưa xác nhận trên cloud: thao tác đó có thể xóa bản nháp duy nhất.

## Những thay đổi đã triển khai

1. Khi `localStorage.setItem()` lỗi quota, đọc lại bản mới trong bộ nhớ thay vì vô tình đọc bản cũ còn nằm trong localStorage.
2. Ghi bản sao IndexedDB theo thứ tự, đợi xác nhận ghi bền vững trước khi đưa bản nháp lên cloud. Nếu cả hai nơi lưu đều thất bại, báo lỗi thay vì báo đã lưu.
3. Sau lệnh ghi cloud, đọc lại revision, thư mục và nội dung canvas. Chỉ xóa `pending` nếu bản đọc lại trùng khớp. Realtime echo đến sớm không thể xác nhận thay cho bước này.
4. Nếu cloud trả bản cũ/khác nội dung khi mở canvas, giữ bản local và yêu cầu chọn phương án khôi phục; bản nháp project chia sẻ từ phiên trước không tự động đẩy lên hoặc bị xóa âm thầm.
5. Canvas khách hiện bản local ngay trong khi IndexedDB đang nạp, nhưng sẽ hiện lỗi nếu lần lưu nền thất bại. Đồng bộ trung tâm chỉ báo “đã đồng bộ” khi trạng thái thật sự `saved`.
6. Badge và service-worker cache đổi sang V5.11.6.

## Kiểm tra sau khi đưa bản cập nhật lên web

1. Sao lưu/export project quan trọng trên profile còn mở được. Không xóa dữ liệu site trước khi đã có bản sao và đã kiểm tra nội dung cloud.
2. Triển khai đúng các file trong `CHANGED_FILES_V5.11.6.txt` theo đường dẫn gốc, build lại frontend và kiểm tra badge V5.11.6. Sau tải lại trang, đảm bảo service worker đã nhận cache mới.
3. Tại profile gặp lỗi, mở DevTools → Application → Storage để xem mức dùng của Cache Storage/IndexedDB. Có thể chạy `navigator.storage.estimate()` trong Console để ước tính `usage` và `quota`; số liệu đó không đủ kết luận `localStorage` có đầy hay không.
4. Vẽ thêm một nét → chờ trạng thái pending → bấm “Đồng bộ ngay” → chỉ chấp nhận “Cloud đã đồng bộ” khi pending về 0. Rời canvas, mở lại và F5; nét phải còn. Lặp lại khi mạng gián đoạn và ở profile có nhiều dự án/media.
5. Trong DevTools, mô phỏng `QuotaExceededError` của localStorage và xác nhận bản nháp vẫn ở IndexedDB sau F5; nếu IndexedDB cũng từ chối ghi, trạng thái phải là lỗi, không phải “đã lưu”.
6. Nếu vẫn báo lỗi, ghi lại thông báo Console/Network của yêu cầu `notes` và số liệu Supabase Dashboard. Quota cloud, chính sách RLS, lỗi SQL 42703 và quota trình duyệt là các nguyên nhân khác nhau, phải phân biệt bằng mã lỗi thực tế.

`navigator.storage.persist()` có thể xin bảo vệ IndexedDB khỏi cơ chế thu hồi dữ liệu của trình duyệt, **không** nâng giới hạn localStorage và không khắc phục bộ nhớ đĩa thực sự hết chỗ. Không tăng quota localStorage bằng client-side JavaScript hoặc bằng cách mua thêm quota cloud.

## Phạm vi xác minh

Test tự động mô phỏng quota, thứ tự các nét, nạp lại cache, readback cloud không khớp và realtime echo. Đã chạy `npm test` (348 test web + 9 test server), `npm run typecheck` và `npm run build` thành công. Chưa kiểm chứng trên Supabase production hoặc trên profile trình duyệt thực tế của bạn; cần làm bước kiểm tra sau triển khai trước khi kết luận lỗi đã hết.
