# Báo cáo phát hành MindCanvas V5.14.2

## Phạm vi

Bản này bắt đầu từ gói V5.14.1 và hoàn thiện xử lý hộp thoại xung đột khi nét vẽ trên thiết bị chưa đồng bộ được với cloud.

## Thay đổi

- Trong lúc một lựa chọn đang chạy, chỉ nút lựa chọn đó bị khóa. Hai phương án còn lại vẫn nhận thao tác và hiện trạng thái xếp hàng.
- Mỗi thời điểm chỉ xử lý một lựa chọn để tránh ghi cloud đồng thời. Nếu thao tác đang chạy thất bại, lựa chọn mới nhất đã xếp hàng sẽ được thử tiếp. Nếu thao tác hiện tại thành công, lựa chọn xếp hàng bị bỏ vì xung đột đã đóng.
- Khi người dùng chọn “Dùng bản cloud”, ứng dụng vẫn tạo mốc khôi phục cho bản local trước khi chuyển sang snapshot cloud mới nhất.
- Giữ xử lý lỗi tại hộp thoại, giữ bản nháp khi ghi đè/lưu bản sao thất bại và cho phép thử lại.
- Nâng version và cache PWA lên V5.14.2 để trình duyệt nhận bundle mới sau deploy.
- Không thay đổi schema và không cần chạy migration SQL.

## Kiểm tra

- Web: 56 file test, 368 kiểm thử đạt.
- Server: 42 kiểm thử đạt.
- Build production web và server: đạt. TypeScript được kiểm tra trong bước build.
- Vite vẫn cảnh báo một số bundle lớn hơn 500 kB; build hoàn tất thành công.

## Phát hành

Bản ZIP đã được tạo từ mã V5.14.1 và cập nhật thành V5.14.2. Chưa deploy lên Render và chưa thử với Supabase thật trên hai trình duyệt; sau khi cập nhật source và deploy, cần xác minh các lựa chọn trên tài khoản bị lỗi thực tế.
