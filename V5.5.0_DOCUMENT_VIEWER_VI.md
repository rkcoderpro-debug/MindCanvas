# MindCanvas v5.5.0 — Document viewer trên canvas

Bản này được áp dụng trên mã v5.3/v5.4 và giữ nguyên cấu trúc `mindcanvas-v1/`.

## Tính năng

- Canvas nhận file **PDF, DOCX, PPTX, XLSX** bằng nút chèn file hoặc kéo thả. File được lưu thành data URL trong board để board xuất/import vẫn tự chứa.
- Viewer PDF render từng trang ngay trên canvas và trong **Quản lý thư mục → Tài liệu đã tải lên**. Có chuyển trang, toàn màn hình, chọn màu/cỡ bút, hoàn tác, xóa nét của trang và tải `*-annotated.pdf` mới.
- DOCX được chuyển sang HTML an toàn để xem; XLSX có tab từng sheet; PPTX có trình bày từng slide và điều hướng.
- Thư viện tài liệu dùng IndexedDB theo tài khoản; khách chưa đăng nhập dùng vùng lưu guest. File có thể xem lại, lọc theo thư mục và xóa trong Quản lý thư mục.
- MIME, kích thước và data URL được kiểm tra khi lưu board. Giới hạn file gốc là 40 MB; viewer và parser Office được tải động để không làm tăng tải ban đầu.

## Cài đặt và chạy

1. Giải nén các file trong ZIP vào đúng thư mục `mindcanvas-v1/`, giữ nguyên đường dẫn.
2. Chạy `npm ci` ở thư mục gốc.
3. Kiểm tra bằng `npm run typecheck`, `npm run test` và `npm run build` theo quy trình dự án hiện tại.

Không cần migration Supabase mới. Dữ liệu tài liệu nằm trên trình duyệt; xóa dữ liệu website sẽ xóa thư viện guest hoặc bản lưu cục bộ tương ứng.

## Ghi chú viewer

- PPTX hiện hiển thị văn bản trong slide và điều hướng slide; bố cục, hình ảnh và biểu đồ phức tạp có thể không giống PowerPoint gốc.
- DOCX/XLSX được sanitize trước khi đưa vào `dangerouslySetInnerHTML`; tài nguyên mạng và script trong nội dung Office không được chạy.
- Nét vẽ PDF được giữ trong viewer của phiên hiện tại. Nút **Xuất PDF** tạo file PDF mới đã nhúng nét vẽ; file gốc không bị ghi đè.
- File Office không hỗ trợ chỉnh sửa nội dung trong viewer; dùng ứng dụng gốc để chỉnh sửa rồi tải lại bản mới.

## Kiểm tra đã chạy

- TypeScript web/server: đạt.
- Production build web: đạt (Vite chỉ cảnh báo một số chunk lớn do thư viện PDF/XLSX).
- Vitest web: **42 file, 274 test đạt**.
- Test bổ sung: lưu/xóa tài liệu theo tài khoản và parse embed PDF; dữ liệu embed độc hại/không hỗ trợ bị từ chối.
