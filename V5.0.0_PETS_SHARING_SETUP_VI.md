# MindCanvas v5.0.0 — thú cưng học tập và chia sẻ học liệu

## Đã triển khai

- Thêm pet companion nổi cố định trên giao diện, hỗ trợ Mèo, Chó, Cáo và Thỏ.
- Pet có tên, cấp độ, XP, sức khỏe 0–100 và trạng thái năng lượng/vui/bình tĩnh/mệt/buồn.
- Thời gian chỉ được ghi khi trang đang hiển thị và người dùng có tương tác gần đây. Mỗi 5 phút học cộng sức khỏe; tối đa 24 điểm mỗi ngày.
- Sau 48 giờ không hoạt động, sức khỏe giảm theo ngày; sau 7 ngày trạng thái chuyển buồn. Dữ liệu local vẫn hoạt động khi offline/khách, tài khoản đăng nhập đồng bộ qua RPC.
- Nút Chia sẻ Flashcard chỉ còn ở phần chi tiết bộ thẻ đang chọn, không còn trong thanh bên.
- Nút Chia sẻ Lab chỉ còn ở phần chi tiết Lab đang chọn, không còn trong danh sách Lab bên trái.
- Sửa 0018: thay các điều kiện `... < CASE ...` gây lỗi `42601` bằng hàm `learning_required_tier`, đồng thời làm migration có thể chạy lại an toàn và yêu cầu PostgREST tải lại schema.

## Migration Supabase

Chạy theo thứ tự trong SQL Editor:

1. Chạy lại toàn bộ `supabase/migrations/0018_v4_9_learning_shares.sql` phiên bản đã sửa.
2. Chạy `supabase/migrations/0019_v5_0_pets.sql`.

0019 tạo `pet_profiles`, `pet_activity_events` và ba RPC:

- `get_my_pet()` — tạo/lấy hồ sơ của tài khoản hiện tại và áp dụng giảm sức khỏe khi vắng mặt.
- `update_my_pet(p_kind, p_name)` — đổi loài hoặc tên pet của chính tài khoản.
- `record_pet_activity(p_event_id, p_seconds, p_activity_type)` — ghi tối đa 300 giây mỗi event, chống ghi trùng theo ID.

Client không được ghi trực tiếp vào hai bảng; RLS chỉ cho đọc bản ghi của chính tài khoản. HTML Lab vẫn chạy trong iframe sandbox/CSP hiện có và không được tự động tải lên tài khoản.

## Kiểm thử đã chạy

- Web Vitest: 255 test, 38 file — đạt.
- Server Node test: 42 test — đạt.
- TypeScript build (`apps/web`, `apps/server`) — đạt.
- Vite production build — đạt.
- Test thuần cho pet: cộng sức khỏe/XP, giảm sức khỏe idempotent, và chuyển mood khi vắng mặt — đạt.

Cần kiểm tra thêm trên thiết bị thật (Safari iOS, Chrome Android và desktop) để xác nhận safe-area của pet, pagehide khi chuyển nền và clipboard/link mời. Đây là kiểm thử thiết bị, không được thay thế bằng Vitest/jsdom.
