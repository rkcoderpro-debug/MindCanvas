# MindCanvas V4.8 — Learning Hub Lab tương tác

V4.8.0 thêm một khu vực **Lab** trong Learning Hub để người học biến tài liệu vật lý, hóa học hoặc khoa học khác thành mô phỏng có thể điều chỉnh.

## Luồng AI Manual hai bước

1. Người dùng chọn môn, trình độ, tài liệu nguồn và mô tả điều muốn mô phỏng. File văn bản nhỏ được đọc cục bộ; PDF/DOCX/PPTX vẫn được giữ tên để người dùng tự tải lên nhà cung cấp AI.
2. MindCanvas tạo prompt yêu cầu AI trả về `mindcanvas-lab-design.json`. JSON có mục tiêu học tập, biến điều khiển, đơn vị, phương trình, quan sát, giả định và lưu ý an toàn.
3. Người dùng dán JSON trở lại Lab. Ứng dụng kiểm tra schema, sau đó tạo prompt bước 2 yêu cầu AI trả về một HTML tự chứa.
4. Người dùng dán HTML, MindCanvas kiểm tra và chạy trong iframe `sandbox="allow-scripts"`. Không cấp `allow-same-origin`, không cho iframe, form, tài nguyên ngoài, network API hoặc truy cập cửa sổ cha.

## Lưu và sử dụng

- Lab được lưu theo tài khoản trên trình duyệt với khóa riêng cho từng owner; khách chưa đăng nhập dùng vùng `guest`.
- Có thể tạo Lab mới, mở bản đã lưu, nạp lại bản đã lưu, xóa Lab và tải HTML về máy.
- Runner chỉ nhận HTML tự chứa, phù hợp với mô phỏng dùng SVG hoặc Canvas 2D và JavaScript nội tuyến.
- Bản này chưa tự gọi AI và chưa đồng bộ Lab lên Supabase. Đây là chủ ý để hoàn thiện quy trình AI Manual và lớp cách ly trước khi thiết kế schema cloud.

## Kiểm thử nghiệm thu

- Prompt bước 1 chứa yêu cầu người dùng, tên tài liệu và schema JSON cố định.
- JSON sai định dạng hoặc thiếu trường bắt buộc bị chặn trước bước 2.
- HTML có script ngoài, iframe, URL ngoài, network API hoặc truy cập `parent/top/opener` bị chặn.
- HTML hợp lệ chạy được trong iframe sandbox và không truy cập storage của MindCanvas.
- Lab của hai tài khoản không nhìn thấy nhau; xóa Lab cập nhật danh sách ngay.
- Kiểm tra giao diện desktop, mobile và đổi ngôn ngữ Việt/Anh.
