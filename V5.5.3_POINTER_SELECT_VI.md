# MindCanvas v5.5.3 — V là con trỏ chọn

Bản cập nhật làm phím **V** hoạt động như công cụ con trỏ chọn quen thuộc trên các ứng dụng thiết kế.

## Thay đổi chính

- Giữ **V** để tạm chuyển sang con trỏ chọn, kể cả khi công cụ đang chọn là Bút.
- Bấm phần tử rồi kéo để di chuyển phần tử hoặc nhóm phần tử đang chọn.
- Kéo từ vùng trống trái → phải để chọn các phần tử nằm trọn trong vùng.
- Kéo phải → trái để chọn kiểu crossing, chỉ cần vùng chọn chạm vào phần tử.
- XP-Pen giữ V vẫn chọn/kéo được; cài đặt “Stylus chỉ dùng để vẽ” không còn biến V thành Bút.
- Con trỏ, biểu tượng thanh công cụ và hướng dẫn canvas hiển thị đúng trạng thái con trỏ chọn.
- Cập nhật version web, service-worker cache và thông tin hỗ trợ lên `v5.5.3`.

## Kiểm tra

Chạy từ thư mục gốc:

```bash
npm ci
npm run typecheck
npm run test
npm run build
```

ZIP phát hành chỉ chứa file thay đổi, giữ tiền tố `mindcanvas-v1/` và loại trừ `dist`, `node_modules`, `.env` cùng cache/`tsbuildinfo`.
