# MindCanvas V4.7.2 — Mobile, Drawing, Theme và Fit

## Phạm vi triển khai

V4.7.2 mở rộng bản V4.7.1 theo bốn nhóm:

- Thao tác nhanh trên mobile: nút tròn ở góc trên bên phải canvas, mở các lệnh Sao chép, Dán, Nhân đôi, Xóa, Hoàn tác và Làm lại.
- Eraser vector: tẩy phần giao với nét Pen/Highlight bằng cách cắt thành các đoạn còn lại; không dùng màu nền để che. Một lượt tẩy được ghi thành một thay đổi.
- Kích thước riêng cho Pen, Highlight và Eraser; các giá trị được giới hạn, hiển thị ngay cạnh công cụ và lưu trên thiết bị.
- Mười theme Plus mới, gồm Crimson Ocean và Crimson Midnight; picker trong Cài đặt và thanh bên đều preview liên tục, swatch thể hiện gradient hai màu, các hàng không có khe dọc.
- “Vừa màn hình” tính lại bounds từ phần tử visible hiện tại, bỏ qua phần tử ẩn/hậu duệ nhánh thu gọn, tính connector, nét vẽ và rotation; Fit chỉ thay đổi viewport.

## Tương thích dữ liệu

`DrawingPath` và BoardState cũ vẫn đọc được. Các kích thước bút là tùy chọn giao diện ngoài dữ liệu canvas; nét đã lưu không tự đổi kích thước khi người dùng chỉnh công cụ. Eraser giữ màu, opacity, width, rotation và cờ khóa/ẩn của các đoạn còn lại.

## Quy tắc an toàn

- Canvas read-only không hiển thị cụm sửa nhanh và không cho thao tác thay đổi dữ liệu.
- Phần tử khóa hoặc ẩn không bị duplicate, xóa hay tẩy.
- Clipboard nội bộ dùng bản sao trong phiên khi trình duyệt từ chối quyền clipboard hệ thống.
- Preview theme không ghi localStorage; rời toàn bộ picker hoặc đóng picker sẽ trả về theme đã chọn.
- Fit, pan và zoom không tạo bước Undo; thao tác tẩy, dán, nhân đôi và xóa dùng lịch sử thay đổi chung của workspace.

## Kiểm thử nghiệm thu

- Unit/component web: chạy toàn bộ Vitest, bao gồm tách nét ở giữa, nét khóa/ẩn, lưu size riêng, quick actions, theme preview liên tục và Fit với viewport cũ/cây thu gọn.
- Server: chạy toàn bộ Node test.
- TypeScript: typecheck web.
- Production: build frontend/server theo script của repository.
- Thiết bị: cần kiểm tra thủ công Chrome/Edge desktop, Android Chrome và iPhone Safari/PWA ở các kích thước 320, 390, 480px và desktop; kết quả mô phỏng trình duyệt được phân biệt với thiết bị thật.

## Phát hành

- Runtime/API release: `4.7.2`.
- PWA shell cache: `mindcanvas-shell-v4.7.2`.
- Không đưa `node_modules`, `dist`, cache, `.env` hoặc file môi trường vào gói thay đổi.
