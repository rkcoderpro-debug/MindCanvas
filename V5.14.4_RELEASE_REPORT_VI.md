# Báo cáo phát hành MindCanvas V5.14.4

## Phạm vi

Bản này nối tiếp V5.14.3 theo yêu cầu không hiện hộp thoại xung đột khi ứng dụng tự dùng bản cloud.

## Thay đổi

- Xóa `CloudConflictDialog` khỏi luồng giao diện; xung đột tự dùng phiên bản cloud mới nhất một lần cho mỗi project.
- Khóa canvas ở chế độ chỉ xem trong lúc xử lý để tránh người dùng vẽ thêm trong khi snapshot đang được thay thế.
- Hiển thị tiến trình dưới dạng thông báo nội tuyến, không phủ màn hình.
- Khi cloud không truy cập được, hiển thị lỗi cùng nút thử lại trên trang; bản nháp local vẫn ở cache khi thao tác thất bại.
- Trước khi chuyển sang cloud, ứng dụng tiếp tục tạo mốc khôi phục cho bản thiết bị theo luồng hiện có.
- Nâng version ứng dụng và Service Worker cache lên V5.14.4 để trình duyệt tải bundle mới sau deploy.
- Không thay đổi schema và không cần chạy migration SQL.

## Kiểm tra

- Web: 57 file test, 372 kiểm thử đạt.
- Server: 42 kiểm thử đạt.
- Build production web và server: đạt; TypeScript được kiểm tra trong build.
- Kiểm thử mới xác nhận tự chọn cloud một lần, không render dialog và không tự lặp lại khi lần thử thất bại.
- Vite vẫn cảnh báo một số bundle JavaScript lớn hơn 500 kB; build hoàn tất thành công.

## Phát hành

Gói ZIP V5.14.4 gồm danh sách file thay đổi kế thừa từ V5.14.3 và thay đổi của bản này. Chưa deploy lên Render và chưa kiểm tra với project cloud thật trên hai profile trình duyệt.
