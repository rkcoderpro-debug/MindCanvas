# MindCanvas v5.11.1 — Kế hoạch kiểm tra và sửa lỗi lưu/tải Cloud

Ngày: 23/09/2026  
Phạm vi: lỗi lưu hoặc tải dữ liệu flashcard khi bộ nhớ trình duyệt đã đầy.

## 1. Bằng chứng từ ảnh

- DevTools Network trong ảnh hiển thị các yêu cầu `flashcard_decks`, `flashcards` và `lab_projects` trả mã `200`. Ảnh chưa cho thấy response body hoặc xác nhận dữ liệu đã được đọc lại sau khi lưu, nhưng không có dấu hiệu lỗi HTTP ở các dòng đang thấy.
- Thông báo lỗi trình duyệt ghi rõ `Storage.setItem` không thể ghi khóa `mindcanvas:flashcards:v1:<user-id>:decks` vì vượt quota.
- Vì vậy lỗi quan sát được là lỗi ghi cache flashcard cục bộ. Nếu xảy ra sau một phản hồi cloud thành công, mã cũ có thể bắt lỗi cache như lỗi cloud: tải dữ liệu sẽ rơi về cache cục bộ; lưu dữ liệu có thể báo thất bại dù lệnh upsert đã hoàn tất.
- Ảnh ứng dụng ghi phiên bản `v5.10.1`; mã nguồn bàn giao đang ở `v5.11.0`. Gói vá được đánh dấu `v5.11.1` để nhận biết bản đã cập nhật và đổi khóa cache Service Worker.

## 2. Sửa chữa đã thực hiện

1. Tách kết quả cloud khỏi cache cục bộ: nếu Supabase đọc hoặc ghi thành công, lỗi `localStorage` không còn biến thao tác đó thành lỗi cloud.
2. Khi ghi cache thất bại, giữ bản cache flashcard theo từng tài khoản trong bộ nhớ của phiên hiện tại để giao diện tiếp tục đọc được dữ liệu vừa nhận.
3. Với thao tác chỉ lưu trên thiết bị, kiểm tra kết quả ghi bền vững. Nếu trình duyệt hết dung lượng, trả lỗi rõ ràng thay vì báo lưu thành công giả.
4. Không thay đổi schema, RLS, migration hay quyền Supabase. Chưa có bằng chứng trong ảnh cần chạy SQL hoặc sửa chính sách cơ sở dữ liệu.
5. Đổi khóa cache Service Worker sang `mindcanvas-shell-v5.11.1` để bản PWA mới không tiếp tục dùng app shell cũ.

## 3. Kế hoạch kiểm tra

| Bước | Cách kiểm tra | Điều kiện đạt |
| --- | --- | --- |
| Kiểm thử hồi quy | Mô phỏng `localStorage.setItem` ném `QuotaExceededError`; tải danh sách bộ flashcard, tải thẻ, lưu bộ và lưu lô thẻ lên cloud | Cloud vẫn trả `source: cloud`, dữ liệu cloud hiện đúng; không phát sinh lỗi quota làm hỏng thao tác |
| Kiểm tra lưu cục bộ | Tắt cloud, mô phỏng bộ nhớ trình duyệt đầy, thử lưu bộ flashcard chỉ trên thiết bị | Giao diện báo không đủ dung lượng và không báo đã lưu bền vững |
| Kiểm tra cloud trên staging | Đăng nhập tài khoản thử nghiệm; tải bộ và thẻ; thêm, sửa, xóa; đọc lại response body và tải lại trang | Dữ liệu khớp Supabase trước và sau khi tải lại; trạng thái giao diện báo cloud; không có fallback về cache cũ khi cloud trả dữ liệu mới |
| Kiểm tra giới hạn cache | Trong môi trường kiểm thử, chặn riêng thao tác ghi khóa cache flashcard; không cố làm đầy hoặc xóa `localStorage` của người dùng thật | Tải và lưu cloud vẫn hoạt động; bản dữ liệu trong phiên vẫn đọc được |
| Kiểm tra Lab riêng | Lưu Lab vào IndexedDB, tải lại trang, tải file HTML xuống, rồi thử tải Lab lên cloud và mở học liệu đã chia sẻ | Bản cục bộ, file HTML và bản cloud được xác minh riêng; không gán lỗi flashcard cache cho Lab |
| Kiểm tra tài khoản | Thử tài khoản chủ sở hữu và một tài khoản khác có quyền phù hợp | Cache được tách theo user ID; không lộ dữ liệu giữa các tài khoản |
| Kiểm tra bản phát hành | Xác nhận app và Service Worker đang chạy v5.11.1 sau khi đưa bản vá lên staging | Không còn phục vụ app shell cũ; sau đó mới xác nhận trên site thật |

## 4. Tiêu chí nghiệm thu

- Cloud trả thành công thì giao diện giữ nguyên dữ liệu cloud dù cache `localStorage` từ chối ghi.
- Tải lại trang vẫn lấy dữ liệu mới từ cloud khi có mạng.
- Khi cloud không dùng được và cache cục bộ cũng không ghi được, giao diện báo lỗi rõ ràng, không hiện trạng thái lưu thành công.
- Tính năng lưu/tải HTML của Lab được kiểm tra độc lập với flashcard.

## 5. Giới hạn kiểm chứng hiện tại

- Đã tái hiện lỗi bằng kiểm thử hồi quy trước khi sửa; các kiểm thử mới hiện đạt.
- Chưa kết nối phiên Supabase thật, chưa kiểm tra response body trên tài khoản staging và chưa triển khai bản vá lên site đang mở trong ảnh. HTTP `200` trong ảnh không tự chứng minh mọi thao tác ghi đã được commit hoặc tải lại thành công.
- Nếu quota vẫn đầy, cache offline lâu dài có thể còn bản cũ cho tới lần cloud tải thành công. Có thể lập bước tiếp theo để chuyển cache flashcard lớn sang IndexedDB sau khi kiểm chứng bản sửa này.
