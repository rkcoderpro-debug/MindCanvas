# MindCanvas V4.7.4 — Eraser, cỡ nét và Undo Text/Shape

V4.7.4 hoàn thiện nhóm công cụ vẽ trên canvas và khóa lại lỗi lịch sử khi tạo phần tử mới.

## Thay đổi chính

### Eraser trên thanh công cụ

- Eraser xuất hiện cạnh Pen và Highlighter trong thanh công cụ mặc định.
- Cấu hình toolbar v1 cũ được migrate sang v2 và tự thêm Eraser nếu người dùng chưa từng bật công cụ này.
- Giữ cơ chế tẩy vector từng phần: chỉ Pen/Highlighter bị tác động; node, text, shape, media và connector không bị xóa nhầm.
- Nét khóa hoặc ẩn được giữ nguyên; mỗi lượt kéo tẩy tạo một commit Undo.

### Thanh cỡ nét gọn và độc lập

- Thanh cỡ Pen, Highlighter và Eraser nằm phía trên canvas, tách khỏi rail toolbar.
- Mỗi công cụ có khoảng giá trị và giá trị nhớ riêng.
- Thanh được thu gọn thành một hàng, dùng icon, nhãn ngắn, slider, giá trị px và mẫu nét/tẩy.
- Track, thumb, progress, viền focus và scrollbar của toolbar dùng màu accent/line của theme đang chọn.
- Khi toolbar ở trái/phải, thanh cỡ nét tự tránh vùng toolbar; trên mobile vẫn chừa vùng fullscreen và safe area.
- Đổi cỡ chỉ lưu tùy chọn giao diện, không tạo Undo và không đổi các nét đã vẽ.

### Undo tạo Text/Shape

- Text và Shape mới đi qua cùng `useWorkspace.change()` với mọi chỉnh sửa canvas khác.
- History stack được đọc từ ref đồng bộ sau commit để nút Undo không bị trễ khi thao tác tạo mới vừa kết thúc.
- Cloud acknowledge hoặc realtime echo của chính phiên hiện tại chỉ cập nhật trạng thái/revision; không xóa Undo.
- Regression test kiểm tra chuỗi tạo Text/Shape → commit → Undo và trường hợp đang chờ đồng bộ.

## Tương thích

- Không đổi BoardState, renderer, định dạng file hoặc schema Supabase.
- Giữ tương thích board cũ, shared/editor/viewer, offline cache, autosave và XPen.
- PWA shell cache đổi thành `mindcanvas-shell-v4.7.4`.

## Kiểm thử nghiệm thu

- Toolbar mặc định có Eraser; cấu hình toolbar v1 được migrate đúng.
- Chọn Pen/Highlighter/Eraser hiển thị thanh cỡ nét nhỏ ở trên canvas; đổi theme cập nhật màu slider và scrollbar.
- Tẩy giữa nét tạo các đoạn vector còn lại, giữ màu/độ đậm và không tạo path rỗng.
- Tạo Text mới rồi Undo làm Text biến mất; tạo Shape mới rồi Undo làm Shape biến mất.
- Tạo/chỉnh sửa → cloud save → realtime echo → Undo vẫn khôi phục trạng thái trước thao tác.
- Chạy toàn bộ web/server test, typecheck và production build.

