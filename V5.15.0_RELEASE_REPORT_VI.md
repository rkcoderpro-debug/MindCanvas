# Báo cáo phát hành MindCanvas V5.15.0

## Phạm vi

Khắc phục hai vấn đề đã nêu trong kế hoạch: tải lại trang làm phiên đăng nhập rơi về khách, và trạng thái lưu canvas báo xung đột dù hai mốc giờ local/cloud gần nhau.

## Đối chiếu kế hoạch

| Yêu cầu | Thay đổi | Kiểm tra |
|---|---|---|
| Giữ đăng nhập sau refresh | Đọc session đã lưu; thử refresh token một lần khi profile từng đăng nhập; không tự vào guest nếu đọc/khôi phục session lỗi; có màn hình đăng nhập lại hoặc tiếp tục guest. Marker tài khoản được ghi khi nhận session và chỉ xóa trước sự kiện sign-out chủ động. | 5 kiểm thử khôi phục session và 2 kiểm thử sign-out. |
| Không đánh dấu đã đồng bộ quá sớm | Sau khi upload, đọc lại cloud theo revision và nội dung canvas. Thử lại theo backoff và dừng sau tối đa 15 giây; chỉ báo đã lưu sau khi nội dung khớp. Lỗi mạng tạm thời được thử lại; lỗi quyền dừng ngay và giữ nháp local. | 5 kiểm thử xác nhận, gồm đọc cloud cũ rồi khớp, nội dung mới hơn gây conflict, lỗi quyền, và giới hạn 15 giây. |
| Không dựa vào timestamp trùng nhau | Timestamp vẫn dùng để hiển thị và làm compare-and-swap ở schema cũ; nó không quyết định xác nhận bản vẽ đã đồng bộ. | Kiểm thử thành công khi mốc giờ khác nhưng revision/nội dung khớp; kiểm thử có timestamp gần nhưng nội dung sai vẫn không được xác nhận. |
| Không hiện popup/banner xung đột lớn | Tự dùng cloud như lựa chọn mặc định; bỏ banner xanh toàn chiều ngang. Trạng thái và lỗi xem trong Trung tâm đồng bộ, với nút trạng thái gọn để mở từ canvas hoặc danh sách project. | Kiểm thử AutoResolveCloudConflict xác nhận không render dialog; build kiểm tra type cho giao diện Sync Center. |
| Không bỏ nháp local nếu chưa có mốc khôi phục | Chỉ chuyển sang bản cloud khi checkpoint chứa đúng nội dung local đã được xác nhận. Sau khi tạo checkpoint, đọc cloud lần nữa để dùng snapshot mới nhất. Nếu lưu snapshot cloud vào cache lỗi, phục hồi cache local và giữ trạng thái chờ đồng bộ. | Kiểm thử checkpoint lỗi giữ nháp local; kiểm thử checkpoint thành công chọn snapshot cloud mới nhất sau thời gian chờ. |
| Phát hành đúng phiên bản | Tăng phiên bản package và Service Worker cache lên 5.15.0. | Đã kiểm tra các package và mindcanvas-shell-v5.15.0. |

## Kết quả kiểm tra

- Web: 60 file test, 385 kiểm thử đạt.
- Server: 42 kiểm thử đạt.
- Build production web và server: đạt.
- Vite vẫn cảnh báo bundle JavaScript lớn hơn 500 kB; build hoàn tất.
- Không thay đổi schema và không cần migration SQL.

## Giới hạn xác minh

Đã kiểm tra bằng unit/integration tests và build tại workspace. Chưa deploy lên Render, chưa thử OAuth hoặc ghi cloud thật trên hai profile trình duyệt, nên xác nhận runtime với dự án Supabase thực tế vẫn cần làm sau khi deploy.

## Phát hành

Gói ZIP V5.15.0 chứa manifest file kế thừa V5.14.4 cùng các file sửa/thêm của bản này. Chưa deploy.
