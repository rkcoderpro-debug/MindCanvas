# MindCanvas V5.14.1 — sửa lưu cloud và xung đột canvas

## Nội dung

- Lưu project có cột revision bằng so sánh revision như trước.
- Nếu database cũ chưa có cột revision, cập nhật project hiện có bằng điều kiện so khớp chính xác `updated_at`; không thử INSERT lên ID của project đang tồn tại.
- Ghi lại mốc cloud sau khi xác minh nội dung, để những lần autosave tiếp theo có đúng nền revision/thời gian.
- Khi cloud đổi trong lúc người dùng đang chọn cách giải quyết, tải phiên bản cloud mới và yêu cầu người dùng xem lại trước khi chọn tiếp.
- Nếu ghi đè hoặc tạo bản sao lỗi, giữ bản nháp, hiện lỗi trong hộp thoại và mở lại nút để thử lại.
- Bỏ phụ thuộc vào cờ bận toàn cục khi giải quyết xung đột.
- Tạo bản sao trước khi ghi cloud để bản nháp vẫn xuất hiện trong Workspace nếu thao tác lưu thất bại.
- Cập nhật badge phiên bản và cache Service Worker sang V5.14.1.

## Kiểm tra

- Toàn bộ kiểm thử web: 56 file, 366 kiểm thử đạt.
- TypeScript typecheck web: đạt.
- Build production web: đạt.
- Vite còn cảnh báo bundle PDF lớn hơn 500 kB; không làm build thất bại.

## Database và phát hành

- Bản này không thêm migration SQL; có thể xử lý cả schema có và chưa có cột `revision`.
- Chưa deploy lên Render. Để badge và PWA trên web đổi sang V5.14.1, cần push các thay đổi lên nhánh Render đang theo dõi và chờ deploy hoàn tất.
