# MindCanvas V5.14.0 — Hamster thú cưng học tập

## Thay đổi

- Bổ sung Hamster vào danh sách loài trong bảng thú cưng, dùng được cho tài khoản khách và tài khoản đăng nhập.
- Vẽ Hamster dạng SVG có thể co giãn: lông cam nhiều sắc độ, mặt và bụng kem, tai/chân hồng, mắt có điểm phản sáng, má, mũi, ria và nét lông.
- Hamster vẫn theo dõi con trỏ, nháy mắt, cúi đầu khi được xoa và chuyển động tai nhẹ. Tôn trọng cài đặt giảm chuyển động của hệ điều hành.
- Thêm migration `0025_v5_14_0_hamster_pet.sql` để cho phép lưu loại `hamster` trong hồ sơ cloud và RPC cập nhật thú cưng.
- Gói triển khai kèm `i18n.tsx` và `flashcardStorage.ts` để xử lý các lỗi TypeScript trong log Render V5.12.0/V5.13.0.
- Đổi phiên bản ứng dụng và cache service worker sang V5.14.0.

## Triển khai

1. Giải nén ZIP từ thư mục cha chứa repository `mindcanvas-v1`, giữ nguyên đường dẫn trong ZIP.
2. Nếu database đã chạy đến migration 0024, chạy một lần `supabase/migrations/0025_v5_14_0_hamster_pet.sql` trong Supabase SQL Editor.
3. Chạy `npm install`, sau đó `npm run build --workspace apps/web`.
4. Commit và push các file thay đổi lên đúng nhánh được Render Auto-Deploy theo dõi.
5. Sau khi deploy thành công, mở bảng thú cưng, chọn Hamster; tải lại trang để xác nhận lựa chọn còn giữ nguyên.

## Kiểm tra

- Đã xóa TypeScript build cache rồi chạy `npm run build --workspace apps/web`: thành công.
- `apps/web/src/lib/pet.test.ts`: 6/6 test thành công, gồm giữ loại Hamster khi đọc hồ sơ pet.
- Vite còn cảnh báo bundle PDF lớn hơn 500 kB; cảnh báo không làm build thất bại.
