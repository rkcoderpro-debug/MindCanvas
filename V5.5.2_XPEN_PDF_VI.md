# MindCanvas v5.5.2 — XP-Pen và PDF fullscreen

Bản cập nhật này bổ sung thao tác giữ phím cho bút XP-Pen và giữ nguyên quy tắc chú thích PDF đã chốt.

## Thay đổi chính

- **Ctrl + kéo bút lên/xuống** zoom canvas theo hướng kéo; tâm zoom cố định tại điểm bút chạm đầu tiên.
- Khi PDF viewer đang fullscreen, Ctrl + kéo zoom riêng PDF theo đúng điểm chạm đầu tiên; Space kéo để pan vùng PDF.
- **Giữ Space** tạm dùng Hand để pan canvas.
- **Giữ E** tạm dùng Tẩy; **giữ H** tạm dùng Highlight; **giữ V** tạm dùng Select/kéo phần tử.
- Thả phím sẽ trả về công cụ đã chọn trước đó; thao tác không làm mất lịch sử Undo/Redo.
- PDF chỉ nhận nét chú thích khi viewer fullscreen. PDF nhúng trong canvas nhận nét bút như một lớp vẽ canvas thông thường và không đưa lớp đó vào file PDF xuất.
- Cập nhật nhãn web, service worker cache và thông tin hỗ trợ lên `v5.5.2`.

## Kiểm tra

Chạy từ thư mục gốc:

```bash
npm ci
npm run typecheck
npm run test
npm run build
```

ZIP phát hành chỉ chứa file thay đổi, giữ tiền tố `mindcanvas-v1/` và loại trừ `dist`, `node_modules`, `.env` cùng cache/`tsbuildinfo`.
