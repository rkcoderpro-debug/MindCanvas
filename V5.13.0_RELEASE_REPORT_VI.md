# MindCanvas V5.13.0 — CDN có kiểm soát cho Lab

## Thay đổi

Lab có thể chạy HTML cần React, Babel, Tailwind, MathJax và các thư viện nằm trên danh sách CDN cho phép. Quyền này mặc định tắt, được lưu theo từng Lab và không tự tải CDN khi mở hoặc khôi phục Lab. Người nhận Lab chia sẻ phải đồng ý trước khi iframe tải các thư viện.

Iframe vẫn chạy với `sandbox="allow-scripts"` và không được cấp `allow-same-origin`. Chính sách CSP chỉ mở script/style/font cho các CDN trong danh sách; `connect-src 'none'` tiếp tục chặn mọi API, bao gồm Gemini. `localStorage` trong nội dung CDN chỉ là bộ nhớ tạm trong phiên. Tài nguyên khác, worker, form và object/embed tiếp tục bị chặn.

File HTML được cung cấp ở dạng bản sao lưu `mindcanvas-labs-backup` để nhập qua nút **Nhập Lab**. Cờ cho phép CDN đã bật trong bản sao; Lab vẫn cần người dùng bấm **Chạy**. Bản HTML gốc được giữ nguyên.

## Cập nhật Supabase

Chạy toàn bộ `supabase/migrations/0024_v5_13_0_lab_cdn_resources.sql` sau migration 0023. Migration thêm cột `allow_external_resources`, cập nhật bộ đếm phiên bản và giữ cờ CDN khi nội dung được chia sẻ hoặc lưu thành bản sao.

Không cần chạy lại migration 0018 hoặc 0021. Không chạy migration này trước 0023.

## Cập nhật ứng dụng

1. Gộp các tệp trong `CHANGED_FILES_V5.13.0.txt` vào mã nguồn.
2. Chạy `npm ci`, `npm run test`, `npm run typecheck`, và `npm run build`.
3. Chạy migration 0024 trên Supabase.
4. Build và phát hành web. Mở lại web/PWA; badge phải hiện **V5.13.0** nhờ cache key service worker mới.
5. Trong Learning Hub → Lab, chọn **Nhập Lab** và nhập `PHANMEMDAYHOC_3D_KHOI_CUTV2_LAB_BACKUP.json`. Bấm vào Lab, xác nhận tùy chọn CDN đã bật, rồi chọn **Chạy**.

## Kiểm tra sau phát hành

- Lab tự chứa cũ vẫn chạy khi tùy chọn CDN tắt.
- File VietGeoAI tải được thư viện React/Babel/Tailwind/MathJax từ CDN đã cho phép.
- Gemini/API bị từ chối trong Console do `connect-src 'none'`; dữ liệu từ phiên đăng nhập MindCanvas không thể được đọc bởi iframe.
- Gửi Lab rồi mở bằng tài khoản nhận: trước khi xác nhận CDN, iframe chưa chạy; sau khi đồng ý, mô phỏng tải bình thường.
- Thay đổi cờ CDN trên Lab làm tăng `content_version`.
- Nhập JSON trên trình duyệt khác cho thấy Lab và cờ CDN vẫn có mặt.

Phát hành này chưa tự triển khai web và chưa chạy migration trên môi trường thật.
