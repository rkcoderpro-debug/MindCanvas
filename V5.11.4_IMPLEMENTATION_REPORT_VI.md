# Báo cáo triển khai v5.11.4

## Phạm vi

Bản này xử lý hai hiện tượng được ghi nhận bằng DevTools: một profile có 6 Lab cá nhân trong IndexedDB `mindcanvas-labs-v2` nhưng profile khác chỉ có Lab mẫu; profile còn lại có thể hiện “Khách” sau F5 dù dữ liệu cục bộ vẫn còn.

## Thay đổi chính

- `apps/web/src/App.tsx`: đồng bộ kết quả `INITIAL_SESSION` với `getSession()`, thêm timeout 10 giây và thông báo khôi phục phiên; không chuyển workspace sang Guest trước khi hoàn tất kiểm tra.
- `apps/web/src/lib/supabase.ts`: trả lỗi thật từ `getSession()` để phân biệt phiên không có với lỗi khôi phục.
- `apps/web/src/lib/learningShare.ts`: đọc đầy đủ Lab đã công bố theo `user_id`.
- `apps/web/src/lib/labStorage.ts`: nhập Lab theo kiểu cộng thêm, giữ bản cục bộ khi trùng ID và nội dung khác.
- `apps/web/src/components/LabPage.tsx`: tự khôi phục Lab cloud còn thiếu, thêm đồng bộ tất cả Lab lên cloud và sao lưu/khôi phục JSON.
- Tăng phiên bản lên `5.11.4`, cập nhật badge qua package version và cache Service Worker `mindcanvas-shell-v5.11.4`.

## Quy tắc an toàn dữ liệu

- Không tự động xoá IndexedDB, localStorage hoặc bản nháp.
- Đồng bộ cloud chỉ chứa các trường Lab đã công bố: tên, môn, trình độ và HTML mô phỏng.
- Nhập cloud hoặc file JSON không ghi đè Lab cục bộ có cùng ID nhưng nội dung khác.
- Bản sao lưu JSON dùng format `mindcanvas-labs-backup`, version `1`.

## Triển khai và kiểm tra thực tế

1. Chạy migration `0021_v5_11_3_learning_version_trigger_fix.sql` trên Supabase production nếu chưa chạy.
2. Deploy web/server v5.11.4.
3. Trên profile có Lab, bấm `Xuất Lab` để giữ bản sao trước khi thao tác trên browser.
4. Đăng nhập cùng tài khoản, bấm `Đồng bộ Lab` trên profile nguồn; mở profile đích và tải lại trang.
5. Xác nhận F5 không đổi tài khoản thành “Khách”; nếu phiên hết hạn, đăng nhập lại rồi kiểm tra Lab cục bộ vẫn còn.

## Giới hạn

Kiểm thử tự động không thay thế kiểm tra trên Brave profile thật, OAuth redirect và Supabase production. Không được coi các cảnh báo `about:srcdoc` trong iframe Lab là lỗi mất dữ liệu.
