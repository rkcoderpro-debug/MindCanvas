# MindCanvas v4.5.1 — chú thích PDF

Bản cập nhật này giữ nguyên cấu trúc `mindcanvas-v1/` và bổ sung trải nghiệm viết trực tiếp trên PDF.

## Thay đổi chính

- Nét bút được vẽ xem trước liên tục trong lúc giữ chuột, touch hoặc bút stylus; không cần chờ thả bút mới thấy nét.
- Có ba công cụ trên thanh PDF: **Bút**, **Highlight** bán trong suốt và **Tẩy**.
- Tẩy cắt phần nét bị chạm, giữ nguyên các đoạn khác của nét và không tác động lên nội dung PDF gốc.
- Ctrl/Cmd+Z hoàn tác và Ctrl/Cmd+Y hoặc Ctrl/Cmd+Shift+Z làm lại lịch sử chú thích khi viewer đang fullscreen.
- Nét highlight và phần tẩy được phản ánh trong file `*-annotated.pdf` khi xuất PDF.
- Service worker và nhãn web được cập nhật theo phiên bản `v4.5.1`.

## Kiểm tra

Chạy từ thư mục gốc:

```bash
npm ci
npm run typecheck
npm run test
npm run build
```

ZIP phát hành chỉ chứa file thay đổi, giữ nguyên tiền tố đường dẫn `mindcanvas-v1/` và không chứa `dist`, `node_modules`, `.env` hoặc cache.
