# MindCanvas v5.7.1 — Guide tương tác có xác nhận hành động

## Mục tiêu

- Nâng schema guide lên `v3`; mỗi bước chỉ hoàn tất sau khi hệ thống nhận đúng hành động tương ứng.
- Khi khu vực cần hướng dẫn chưa mở, overlay giữ trạng thái chờ và không còn nút “Tiếp tục” để bỏ qua. Các guide Lab có bước điều hướng thật qua sidebar và tab Lab.
- Bước nhập liệu kiểm tra giá trị tối thiểu; bước action chờ event thành công từ canvas, Lab, PDF, tài liệu hoặc command palette.
- Bước thực hành cuối hiển thị checklist thao tác, chỉ bật nút hoàn tất khi đủ các action đã khai báo.
- Giữ animation con trỏ ở ngoài vùng focus, cách tối thiểu 16px và hướng vào cạnh của control.
- Bổ sung event telemetry nội bộ cho thao tác vẽ/chọn/di chuyển/pan, tạo prompt HTML, đưa HTML vào runner, chạy/lưu Lab, mở tài liệu, chú thích/xuất PDF và tìm/chọn action AI.
- Cập nhật giao diện và service worker lên v5.7.1.

## Cách dùng

1. Mở `Hướng dẫn tính năng` hoặc để guide tự kích hoạt khi vào một khu vực mới.
2. Làm đúng thao tác đang được viền sáng. Nếu thao tác chưa xảy ra, nút qua bước vẫn bị khóa.
3. Với bước thực hành, hoàn thành từng mục trong checklist; khi action được hệ thống nhận, mục đó sẽ có dấu tích.
4. Admin có thể reset trạng thái guide để kiểm tra lại luồng first-use.

Trạng thái mới lưu ở khóa `mindcanvas:feature-guides:v3:<owner>`, vì vậy nội dung strict flow được phát lại một lần cho người đã hoàn thành bản cũ.

## Kiểm thử

- Frontend: 46 test files, 293 tests passed.
- Kiểm thử riêng overlay: không cho bypass khi target thiếu, chỉ qua sau click thật, checklist practice bắt buộc đủ action.
- Production build: passed.
- Server typecheck và test: 41 passed, 1 skipped.
