# MindCanvas v5.7.4 — Khôi phục con trỏ guide

## Đã sửa

- Xóa nhãn chữ nổi “Bấm vào toàn bộ vùng đang sáng” vì gây che khuất và làm rối thao tác.
- Khôi phục con trỏ guide thành biểu tượng mũi tên nhỏ, nền trong suốt, nằm ngoài vùng mục tiêu.
- Con trỏ chỉ chuyển động nhẹ theo hướng control, không còn ô bo góc màu lớn phủ lên nút cần bấm.
- Giữ nguyên spotlight sáng toàn vùng và lớp làm tối bên ngoài để người dùng vẫn biết chính xác khu vực cần thao tác.
- Cập nhật version web, service-worker cache key và prompt AI lên v5.7.4.

## Kiểm tra

- TypeScript web: đạt.
- Vite production build: đạt.
- Frontend Vitest: 46 file, 296 test đạt.
- Test overlay xác nhận spotlight vẫn dùng `full-region` và không render nhãn chữ.

