# MindCanvas V5.0.0

MindCanvas is a visual study workspace for turning notes and private documents into editable canvases, mind maps and flashcards. V4.0 adds manually administered Free/Plus/Pro/Max plans, daily AI quotas reset at 12:00 Vietnam time, the separate 29,000₫/month AI Manual add-on, subscription history, per-account quota telemetry, a protected admin dashboard and Zalo-based upgrade requests without automatic payments or webhooks. V4.1 refines text editing, diagonal touchpad panning, connector feedback, remaining AI quota visibility, and the canvas navigator/zoom layout. V4.2 adds adaptive AI study plans, material-to-flashcard previews, deck expansion suggestions, ordered/random review, single-card flip/swipe sessions, forgotten-card retries, idempotent offline study events and streaks. V4.3 groups study tools in Learning Hub, adds manual/hybrid daily task planning, four-choice Quiz tests from documents or Gemini Web, quiz attempts and a persistent floating timer. V4.3.1 makes the timer draggable and persistent per mode, exposes AI difficulty/depth presets, makes AI study planning prominent, adds Quiz learn/practice/exam/mistake-review modes with random order, preserves every generated question instead of silently truncating it, and stores the question order for future review. V4.3.2 keeps the hidden timer launcher at its dragged position, fixes two-sided mind-map PNG/SVG connector geometry, and gives Mind Map AI Auto the same difficulty, depth and detail controls as AI Manual. V4.4 adds owner-controlled project sharing with expiring email invitations, editor/viewer roles, RLS-protected collaborators, shared source documents, a Shared with me view, and Postgres Changes updates with revision-safe conflict recovery. V4.5 adds a calmer app shell, faster route loading, shared motion/theme tokens, persistent Focus mode, mobile-friendly surfaces and lightweight online collaborator presence while preserving V4.4 data and permissions. V4.5.1 turns Share into a full responsive workspace page, repairs the collaboration RPC/schema-cache path, maps missing Supabase functions to actionable setup guidance, adds Alt+wheel horizontal canvas panning and a separated mobile canvas control layout. The V4.5.1 runtime follow-up repairs the ambiguous `project_id` RPC references, recreates the `notes` RLS policies, verifies the Supabase token before cloud writes and invalidates the old PWA shell cache. V3.8 server-side AI file pipeline, responsive navigation, temporary theme previews, V3.7.1 media/resize work and the V3.5.1 shared Gemini reliability hotfix remain included. This repository contains a React/Vite frontend, an Express server boundary for private AI/document operations, shared graph types, and Supabase migration scaffolding.

V4.5.6 adds an iOS-only repair layer for WebKit touch strokes, mobile Share access, Share Sheet/clipboard fallbacks, media picker/playback compatibility, narrow safe-area layouts and missing mobile actions. V4.5.7 adds an iOS Safari media-rendering fallback for blank or mis-sized images after canvas pan, while retaining the existing Android/Windows render path. V4.5.8 keeps iOS images in the shared viewport layer, extends phone layouts through 480 CSS px, hides mobile zoom buttons by default with a Settings opt-in, supports Windows/XPen input, adds straight-line and triangle tools, repairs the PWA response cache race and cloud conflict recovery, and lets AI Auto continue in a minimized task. See [V4.5.8_INPUT_CLOUD_UI_UPDATE_VI.md](./V4.5.8_INPUT_CLOUD_UI_UPDATE_VI.md) for scope and QA.

V4.5.9 makes shared projects cloud-authoritative online and read-only from the last cloud snapshot offline, removes stale shared-cache uploads, adds an in-app invitation inbox with accept/decline, exposes AI Manual for study-plan generation, updates Max to 499,000₫/month, repairs mobile Share scrolling and iOS media overlays, and extends XPen drawing to line/triangle tools. Android and Windows rendering/input paths remain unchanged. See [V4.5.9_SHARED_AI_IOS_UPDATE_VI.md](./V4.5.9_SHARED_AI_IOS_UPDATE_VI.md) for implementation, migration and QA.

V4.7.0 focuses on AI-assisted editing of a selected mind-map branch, explicit branch/cross-link relationships, deterministic multi-direction layouts, adaptive connector sides, branch/selection rotation, safe re-parenting and stale-preview protection. Existing BoardState files remain readable because the new edge kind and layout behavior metadata are optional. See [V4.7_MINDMAP_AI_LAYOUT_VI.md](./V4.7_MINDMAP_AI_LAYOUT_VI.md) for the implementation scope, acceptance matrix and manual QA.

V4.7.1 makes every applied mind-map layout/rotation change a synchronous, single-step Undo transaction and exposes Hoàn tác directly in the layout report. It replaces the fixed gray project-card metadata area with theme-derived colors, gives Plus themes curated two-color gradients in both the live shell and theme previews, and advances the PWA/API release key to `4.7.1`. See [V4.7.1_THEME_UNDO_VI.md](./V4.7.1_THEME_UNDO_VI.md) for scope and QA.

V4.7.2 adds mobile quick actions, vector partial erasing with independent Pen/Highlight/Eraser sizes, ten additional Plus themes including red-blue Crimson variants, gap-free theme preview rows, and a visible-content-only canvas Fit recalculation. See [V4.7.2_MOBILE_DRAWING_THEME_FIT_VI.md](./V4.7.2_MOBILE_DRAWING_THEME_FIT_VI.md) for scope and QA.

V4.7.3 restores free two-axis touchpad panning and keeps the local Undo/Redo transaction history when the cloud acknowledgement or realtime echo changes the status from saving to saved. See [V4.7.3_TOUCHPAD_SYNC_UNDO_VI.md](./V4.7.3_TOUCHPAD_SYNC_UNDO_VI.md) for scope and QA.

V4.7.4 keeps newly created Text and Shape elements in the same undoable workspace transaction, exposes Eraser in migrated toolbars, and moves the compact Pen/Highlight/Eraser size control above the canvas with theme-synced slider and scrollbar colors. See [V4.7.4_DRAWING_TOOL_UNDO_VI.md](./V4.7.4_DRAWING_TOOL_UNDO_VI.md) for scope and QA.

V4.8.1 keeps Undo/Redo after the current browser's cloud save is acknowledged, including when the cloud response canonicalizes document metadata. It also changes Learning Hub Lab to the intended Manual flow: source and request generate a copyable planning prompt, the external AI returns a detailed HTML implementation prompt with a deterministic test plan, and the user then pastes or uploads the resulting self-contained HTML into the sandbox runner. See [V4.8.1_UNDO_LAB_VI.md](./V4.8.1_UNDO_LAB_VI.md) for scope and QA.

V4.8.2 simplifies Lab to two steps: create one direct prompt that asks the external AI to return the finished HTML file, then upload or paste and run that file. It accepts standard inline SVG namespaces without mistaking them for network resources, moves Lab into a highlighted Learning Hub tab and keeps the old Lab route compatible. The runner now has a longer responsive iframe, a real shell-level fullscreen toggle with Escape synchronization, and can save an HTML-only Lab before the source/request section is complete. See [V4.8.2_LAB_HTML_HUB_VI.md](./V4.8.2_LAB_HTML_HUB_VI.md) for scope and QA.

V4.8.0 added Learning Hub Lab with a sandboxed HTML runner. Existing saved Labs remain readable through compatibility fields.

V5.0.0 adds the account-scoped study companion, which gains vitality from active study and becomes tired or sad after a long absence. It also places Flashcard and Lab sharing on the selected material detail only, repairs the PostgreSQL tier-expression error in `0018_v4_9_learning_shares.sql`, and adds the pet profile/activity RPCs in `0019_v5_0_pets.sql`. See [V5.0.0_PETS_SHARING_SETUP_VI.md](./V5.0.0_PETS_SHARING_SETUP_VI.md) for the migration order and QA notes.

## Run locally

```bash
npm install
cp .env.example .env
npm run dev
```

The frontend runs on Vite's default port and the API runs on `http://localhost:8787`. Without Supabase credentials, the app intentionally uses a blank local canvas. With Supabase configured, projects, folders, notes and source documents are scoped to the signed-in user through RLS. `.doc`/`.ppt` legacy files are intentionally rejected; export them as `.docx`/`.pptx` first.

See [PROJECT_HANDOFF.md](./PROJECT_HANDOFF.md) for architecture, setup, credentials, current limits, and the next implementation slices.

Để cấu hình Supabase, Google OAuth và deploy hai service lên Render, làm theo [DEPLOY_V1_VI.md](./DEPLOY_V1_VI.md).
