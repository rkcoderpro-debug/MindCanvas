# Báo cáo phát hành MindCanvas V5.14.3

## Phạm vi

Bản này kế thừa gói V5.14.2 và đổi hành vi xử lý xung đột theo yêu cầu: ưu tiên tự dùng bản cloud mới nhất.

## Thay đổi

- Hộp thoại xung đột tự chọn “Dùng bản cloud” ngay khi phát hiện xung đột.
- Tạo mốc khôi phục cho bản thiết bị trước khi thay snapshot đang mở bằng bản cloud mới nhất.
- Lỗi đọc lại cloud sau khi ghi nhưng nội dung/revision không khớp được phân loại là xung đột; lỗi mạng hoặc quyền truy cập vẫn hiển thị lỗi và không bị coi nhầm là xung đột.
- Nếu thao tác tự dùng cloud thất bại, hộp thoại vẫn cho chọn phương án phục hồi; các thao tác thay thế chạy tuần tự, không ghi cloud song song.
- Nâng version ứng dụng và Service Worker cache lên V5.14.3 để trình duyệt nhận bundle mới sau deploy.
- Không thay đổi schema, không cần migration SQL.

## Kiểm tra

- Web: 56 file test, 370 kiểm thử đạt.
- Server: 42 kiểm thử đạt.
- Build production web và server: đạt; TypeScript được kiểm tra trong bước build.
- Vite cảnh báo bundle JavaScript lớn hơn 500 kB; build hoàn tất thành công.

## Phát hành

ZIP V5.14.3 đã được tạo từ V5.14.2. Chưa deploy lên Render và chưa kiểm tra với project cloud thật trên hai profile trình duyệt.
