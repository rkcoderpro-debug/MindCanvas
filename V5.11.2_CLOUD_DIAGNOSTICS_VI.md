# v5.11.2 — Giữ nguyên lỗi cloud khi lưu dự phòng thất bại

## Bằng chứng mới
Ảnh web hiển thị nhiều POST /rest/v1/flashcards trả HTTP 400 và cảnh báo không lưu bền vững trên trình duyệt. Ảnh không chứa Response JSON, nên chưa xác định được nguyên nhân server từ status 400.

## Đã sửa trong mã
- Giữ mã và message của lỗi cloud khi lưu bộ thẻ, một thẻ hoặc một lô thẻ.
- Nếu cloud từ chối bằng mã lỗi, báo rõ chưa đồng bộ, kể cả khi đã ghi được bản dự phòng.
- Nếu cả cloud và bộ nhớ trình duyệt thất bại, hiển thị cả hai vấn đề; không báo thành công.
- Giữ sửa lỗi v5.11.1: cache đầy không làm mất kết quả lưu/tải cloud thành công.
- Không tự xóa dữ liệu trình duyệt, không đổi schema hay quyền truy cập.

## Kế hoạch kiểm tra và sửa tiếp
1. Trong DevTools > Network > Fetch/XHR, chọn POST flashcards màu đỏ > Response. Thu thập code, message, details, hint; không gửi Authorization, cookie hoặc access_token.
2. Đối chiếu Response với payload và schema thực tế. Kiểm tra UUID, quan hệ deck/project, giới hạn front/back, source_page và các trường lịch ôn; chỉ sửa dữ liệu/schema sau khi xác định lỗi cụ thể.
3. Kiểm tra migrations đã áp dụng trên Supabase. Không chạy lại hàng loạt migration chỉ dựa vào HTTP 400.
4. Sau sửa, lưu một thẻ rồi một lô thẻ; đọc lại bằng phiên đăng nhập trên trình duyệt khác. Xác minh nội dung và lịch ôn khớp.
5. Mô phỏng cache đầy: lưu cloud thành công vẫn báo thành công. Cloud bị từ chối phải báo mã lỗi và trạng thái dự phòng.
6. Kiểm tra offline và quyền tài khoản khác. Chưa xác nhận đồng bộ tự động cho bản dự phòng; sao lưu nội dung trước khi tải lại/xóa dữ liệu site.

## Triển khai và giới hạn
ZIP chỉ chứa file thay đổi, tiền tố mindcanvas-v1/. Áp dụng trên cây mã v5.11.1 tương ứng. Đây là bản sửa xử lý/thông báo lỗi; chưa phải xác nhận đã sửa nguyên nhân HTTP 400 trên production. Chưa deploy và chưa kiểm thử Supabase thật. Không cần SQL cho bản này.

## Kiểm chứng tự động
- 4 kiểm thử mới thất bại trước sửa và đạt sau sửa.
- Toàn bộ web: 51 file, 340 kiểm thử đạt.
- Typecheck web và server: đạt.
- Build web và server: đạt; còn cảnh báo kích thước chunk >500 kB.
